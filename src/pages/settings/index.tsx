import { View, Text } from '@tarojs/components';
import './index.css';

export default function Settings() {
  return (
    <View className="settings-page">
      <Text>设置 - 仅小程序可见</Text>
    </View>
  );
}
